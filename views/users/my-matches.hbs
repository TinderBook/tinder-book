// vista de matches
<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h1>Matches</h1>
      <ul>
        {{#each matches}}
          <li>
            <a href="/users/{{this._id}}">{{this.name}}</a>
          </li>
        {{/each}}
      </ul>
    </div>
  </div>