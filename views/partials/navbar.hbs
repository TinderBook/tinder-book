<nav class="navbar navbar-expand-lg bg-light">
  <div class="container">
    <a class="navbar-brand" href="/dashboard">TinderBook</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#main-nav"
     aria-controls="main-nav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="main-nav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        {{#if currentUser}}
        <li class="nav-item"><a class="nav-link {{navActive navigationPath '/user/profile'}}" aria-current="page" href="/user/profile">Perfil</a></li>

        <li class="nav-item">
           <a class="nav-link {{navActive navigationPath '/users/my-matches'}}" aria-current="page" href="/users/my-matches">Matches 
           {{#if unnotifiedMatches}}
           <span class="badge bg-danger">{{unnotifiedMatchesCount}}</span>
           {{/if}}
           </a>
        </li>

        {{#if unreadMessages}}
        <li class="nav-item">
           <a class="nav-link" href="/path-to-messages">Mensajes 
           <span class="badge bg-danger">{{unreadMessagesCount}}</span>
           </a>
        </li>
        {{/if}}

        {{/if}}
      </ul>
      <ul class="navbar-nav">
        {{#if currentUser}}
            {{#ifEqual navigationPath "/dashboard"}}
            <li class="nav-item"><a class="nav-link me-2 {{navActive navigationPath '/user/profile'}}" aria-current="page" href="/user/profile">Hello {{currentUser.name}}</a></li>
            {{else}}
            <li class="nav-item"><a class="nav-link me-2 {{navActive navigationPath '/users/my-matches'}}" aria-current="page" href="/user/profile">Hello {{currentUser.name}}</a></li>
            {{/ifEqual}}

            {{#ifEqual navigationPath "/user/profile"}}  
                <a class="nav-link btn btn-secondary btn-sm" aria-current="page" href="/user/{{user.id}}/editProfile">Edit</a>
            {{/ifEqual}}
                
            <form action="/logout" method="POST">
                <button type="submit" class="nav-link btn btn-secondary btn-sm" aria-current="page">Log out</button>
            </form>
        {{else}}
            <li class="nav-item"><a class="nav-link {{navActive navigationPath '/register'}}" aria-current="page" href="/register">Register</a></li>
            <li class="nav-item"><a class="nav-link {{navActive navigationPath '/login'}}" aria-current="page" href="/login">Login</a></li>
        {{/if}} 
      </ul>
    </div>
  </div>
</nav>
